{
  "name": "wrio-dev",
  "version": "1.0.0",
  "description": "Start package for local development",
  "dependencies": {},
  "devDependencies": {
    "http-server": "^0.8.0"
  },
  "scripts": {
    "build": "hs -o && cd WRIO-InternetOS && npm i && npm run build",
    "watch": "hs -o && cd WRIO-InternetOS && npm i && npm run watch"
  },
  "author": "spirin.vladimir@gmail.com",
  "license": "ISC"
}
